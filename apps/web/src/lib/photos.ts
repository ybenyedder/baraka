/**
 * Photos culinaires fictives — désormais mutualisées dans `@baraka/shared`
 * (mêmes visuels sur web et mobile). Réexport pour ne pas casser les imports
 * existants (`@/lib/photos`).
 */
export { foodPhoto } from '@baraka/shared';
